# 鑫澜消防器材有限公司 - 官方网站

这是一个使用 **Vue 3 + Vue CLI** 开发的现代化响应式网站，展示公司资质和产品信息。

## ✨ 项目特点

- 🎯 **Vue 3** - 使用最新的Vue 3 Composition API
- 📦 **组件化开发** - 10个独立的Vue单文件组件
- 📱 **响应式设计** - 完美适配手机、平板和桌面设备
- 🎨 **现代化UI** - 参考优秀网站设计，美观专业
- 🔍 **产品筛选** - 支持多分类产品展示和筛选
- 📲 **二维码生成** - 一键生成网站二维码供手机访问
- ⚡ **性能优化** - 使用Vue CLI构建优化
- 🎭 **平滑动画** - CSS3动画效果流畅自然

## 📂 项目结构

```
d:\front\
├── public/                 # 静态资源目录
│   └── index.html         # HTML模板
├── src/                   # 源代码目录
│   ├── assets/           # 资源文件
│   │   ├── images/       # 图片资源
│   │   │   └── products/ # 产品图片
│   │   └── styles/       # 样式文件
│   │       └── global.css # 全局样式
│   ├── components/       # Vue组件
│   │   ├── Header.vue              # 页头组件
│   │   ├── HeroSection.vue         # 首页Banner
│   │   ├── AboutSection.vue        # 关于我们
│   │   ├── QualificationsSection.vue # 公司资质
│   │   ├── ProductsSection.vue     # 产品展示
│   │   ├── CasesSection.vue        # 工程案例
│   │   ├── ContactSection.vue      # 联系我们
│   │   ├── QRCodeFloat.vue         # 二维码悬浮按钮
│   │   ├── BackToTop.vue           # 返回顶部
│   │   └── Footer.vue              # 页脚
│   ├── data/             # 数据文件
│   │   └── siteData.js   # 网站所有数据配置
│   ├── App.vue           # 根组件
│   └── main.js           # 入口文件
├── .gitignore            # Git忽略文件
├── babel.config.js       # Babel配置
├── package.json          # 项目依赖
├── vue.config.js         # Vue CLI配置
└── README.md             # 项目说明

```

## 🚀 快速开始

### 环境要求

- Node.js 14.x 或更高版本
- npm 6.x 或更高版本

### 安装依赖

```bash
npm install
```

### 开发模式运行

```bash
npm run serve
```

项目将运行在 `http://localhost:8080`

### 生产环境构建

```bash
npm run build
```

构建后的文件将输出到 `dist/` 目录

### 代码检查

```bash
npm run lint
```

## 📱 二维码功能

网站集成了二维码生成功能：

1. **开发环境使用**：
   - 运行 `npm run serve` 后，网站会在 `http://localhost:8080` 启动
   - 点击右下角的红色二维码按钮
   - 在同一局域网内的设备可以扫码访问（需要使用电脑的局域网IP）

2. **生产环境使用**：
   - 部署到服务器后，二维码会自动生成实际的网站URL
   - 用户扫码即可直接访问

## 🎨 自定义配置

### 修改公司信息

编辑 `src/data/siteData.js` 文件：

```javascript
// 修改联系信息
export const contactInfo = [
  {
    icon: 'fas fa-map-marker-alt',
    title: '公司地址',
    content: '您的公司地址'  // 在这里修改
  },
  {
    icon: 'fas fa-phone',
    title: '联系电话',
    content: '您的联系电话'  // 在这里修改
  },
  // ...
]
```

### 添加产品

在 `src/data/siteData.js` 的 `products` 数组中添加：

```javascript
export const products = [
  // 添加新产品
  {
    id: 12,  // 新的唯一ID
    name: '产品名称',
    description: '产品描述',
    image: require('@/assets/images/products/图片文件名.jpg'),
    category: 'extinguisher',  // 分类：extinguisher/hydrant/equipment/accessories
    tag: '新品'  // 可选标签
  },
  // ...
]
```

### 修改颜色主题

编辑 `src/assets/styles/global.css`：

```css
:root {
  --primary-color: #1e5ba8;      /* 主色调（蓝色） */
  --secondary-color: #e74c3c;    /* 强调色（红色） */
  --text-dark: #333;             /* 深色文字 */
  --text-light: #666;            /* 浅色文字 */
  --bg-light: #f5f5f5;          /* 浅色背景 */
}
```

### 添加产品图片

1. 将图片放入 `src/assets/images/products/` 目录
2. 在 `src/data/siteData.js` 中使用 `require('@/assets/images/products/图片名.jpg')` 引用

## 🌐 部署到线上

### 方法一：使用 Netlify（推荐）

1. 注册 [Netlify](https://www.netlify.com/) 账号
2. 连接GitHub仓库或直接拖拽 `dist` 文件夹
3. 构建命令：`npm run build`
4. 发布目录：`dist`
5. 自动部署完成！

### 方法二：使用 Vercel

1. 注册 [Vercel](https://vercel.com/) 账号
2. 导入GitHub仓库
3. 框架选择：Vue.js
4. 自动检测配置并部署

### 方法三：使用 GitHub Pages

1. 修改 `vue.config.js` 中的 `publicPath`：
   ```javascript
   publicPath: process.env.NODE_ENV === 'production'
     ? '/你的仓库名/'
     : '/'
   ```
2. 运行 `npm run build`
3. 将 `dist` 目录内容推送到 `gh-pages` 分支
4. 在仓库设置中启用 GitHub Pages

### 方法四：传统虚拟主机

1. 运行 `npm run build` 生成 `dist` 目录
2. 将 `dist` 目录中的所有文件上传到服务器
3. 确保服务器配置了 SPA 路由支持（重定向到 index.html）

## 🔧 技术栈

- **Vue 3** - 渐进式 JavaScript 框架
- **Vue CLI 5** - Vue 官方脚手架工具
- **CSS3** - 现代化样式和动画
- **Font Awesome 6** - 图标库
- **QRCode** - 二维码生成库

## 📦 依赖包

```json
{
  "dependencies": {
    "core-js": "^3.8.3",
    "qrcode": "^1.5.3",
    "vue": "^3.2.13"
  },
  "devDependencies": {
    "@vue/cli-service": "~5.0.0",
    "@vue/cli-plugin-babel": "~5.0.0",
    "@vue/cli-plugin-eslint": "~5.0.0",
    "eslint": "^7.32.0",
    "eslint-plugin-vue": "^8.0.3"
  }
}
```

## 💡 开发说明

### 组件说明

- **Header.vue**: 顶部导航栏，包含Logo和菜单
- **HeroSection.vue**: 首页大图横幅
- **AboutSection.vue**: 公司介绍和统计数据
- **QualificationsSection.vue**: 公司资质展示
- **ProductsSection.vue**: 产品展示和分类筛选
- **CasesSection.vue**: 工程案例展示
- **ContactSection.vue**: 联系方式信息
- **QRCodeFloat.vue**: 悬浮二维码按钮
- **BackToTop.vue**: 返回顶部按钮
- **Footer.vue**: 页脚信息

### 数据管理

所有网站数据统一在 `src/data/siteData.js` 中管理，方便维护和修改。

### 样式系统

- 全局样式：`src/assets/styles/global.css`
- 组件样式：每个组件的 `<style scoped>` 部分
- CSS变量：统一的颜色和尺寸变量

## 🐛 常见问题

### 问题1：npm install 失败

```bash
# 尝试清除缓存
npm cache clean --force
npm install
```

### 问题2：端口被占用

修改 `vue.config.js` 中的端口号：

```javascript
devServer: {
  port: 8081,  // 改为其他端口
}
```

### 问题3：图片无法显示

确保图片路径正确，使用 `require('@/assets/images/...')` 或将图片放在 `public` 目录

### 问题4：生产环境路径问题

检查 `vue.config.js` 中的 `publicPath` 配置是否正确

## 📞 技术支持

如需技术支持或有任何问题，请联系开发人员。

## 📄 许可证

本项目为私有项目，版权归 **鑫澜消防器材有限公司** 所有。

---

**最后更新**: 2026年1月  
**版本**: 1.0.0  
**框架**: Vue 3 + Vue CLI 5




